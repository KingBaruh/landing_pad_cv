# הוראות התקנה והרצה

העבודה מזהה משטח נחיתה בצורת דף A4 עם X, עוקבת אחריו ומחשבת מיקום ותנוחה ביחס למצלמה. ההרצה המלאה מפעילה שלושה תהליכים ושומרת תוצאות, תמונות מדגמיות וגרפי ביצועים.

**להרצת המערכת המלאה משתמשים ב-`main.py`. אין צורך להריץ קודם את תוכניות ההדגמה של חלק 1 וחלק 2 או לחשב מחדש את הכיול בכל הרצה.**

## הכנת סביבת העבודה

נדרשים Python 3, חבילות הפרויקט, סרטון קלט וקובץ פרמטרי מצלמה התואם לצילום. הפקודות כאן מיועדות ל-Windows.

פתחו מסוף בתיקיית הפרויקט, שבה נמצא `main.py`. יש לבצע את כל הפקודות מתוך תיקייה זו, כדי שהנתיבים היחסיים יפעלו גם לאחר העתקת הפרויקט למחשב אחר.

צרו סביבה וירטואלית, אם עדיין אינה קיימת:

```text
python -m venv .venv
```

הפעילו אותה לפי סוג המסוף.

ב-CMD:

```bat
.venv\Scripts\activate.bat
```

ב-PowerShell:

```powershell
.\.venv\Scripts\Activate.ps1
```

לאחר ההפעלה התקינו את החבילות:

```text
python -m pip install -r requirements.txt
```

החבילות הנדרשות הן `opencv-python`, `numpy` ו-`matplotlib`. אם הסביבה כבר מותקנת ופעילה, אפשר לעבור ישירות להרצה.

אפשר גם להשתמש ישירות ב-Python של הסביבה, ללא הפעלתה:

```text
.venv\Scripts\python.exe -m pip install -r requirements.txt
.venv\Scripts\python.exe main.py --video videos/v1.mp4 --output outputs/run_v1
```

## הרצה מלאה

```text
python main.py --video videos/v1.mp4 --output outputs/run_v1
```

- `--video` מציין את סרטון הקלט.
- `--output` מציין את תיקיית התוצאות, שנוצרת לפי הצורך.
- פרמטרי המצלמה נטענים כברירת מחדל מ-`calibration/camera_params.npz`.
- נפתח חלון המציג את הפריימים שעובדו, את מצב המעקב ואת נתוני ה-Pose כשיש אומדן תקין.
- בסיום נוצרים גם גרפי הביצועים בתוך תיקיית הפלט.

**לכל הרצה שרוצים לשמור יש לבחור תיקיית פלט חדשה.** שימוש חוזר באותו נתיב עשוי לדרוס תוצאות ולהשאיר תמונות מדגמיות מהרצה קודמת. תיקיות `outputs/experiment_*` מכילות את הניסויים שעליהם מבוסס המסמך הטכני; הדוגמאות כאן משתמשות בשמות אחרים.

לניסוי נוסף:

```text
python main.py --video videos/v8_landing.mp4 --output outputs/run_v8_landing
```

סרטוני הניסויים הם `v1.mp4` עד `v7.mp4`, וכן `v8_landing.mp4`, בתיקיית `videos`. אפשר לבחור כל אחד מהם באותה צורה.

## אפשרויות שימושיות

הרצה ללא חלון תצוגה:

```text
python main.py --video videos/v1.mp4 --headless --output outputs/run_v1_headless
```

בדיקה קצרה המוגבלת ל-120 פריימי מקור:

```text
python main.py --video videos/v1.mp4 --max-frames 120 --output outputs/run_v1_short
```

זו ריצה חלקית, ואין להשתמש בתוצאותיה כאילו כל הסרטון נבדק. ללא `--max-frames`, ברירת המחדל היא לקרוא עד סוף הסרטון.

בחירת קובץ כיול מפורש:

```text
python main.py --video videos/v1.mp4 --camera-params calibration/camera_params.npz --output outputs/run_v1_explicit
```

לכל אפשרויות ההרצה:

```text
python main.py --help
```

| אפשרות | ברירת מחדל | משמעות |
|---|---|---|
| `--output` | `outputs/runtime` | תיקיית התוצאות |
| `--width` | `1280` | רוחב העבודה המרבי, תוך שמירת יחס התמונה |
| `--save-every` | `30` | מרווח מזהי פריימים לשמירת תמונות פלט מדגמיות |
| `--slow-interval` | `1.0` | מרווח בשניות מקור בין הזדמנויות לזיהוי מלא |
| `--headless` | כבוי | ביטול חלון התצוגה; תוצאות ותמונות מדגמיות עדיין נשמרות |

אין צורך לשנות את הגדרות ברירת המחדל לצורך ההרצה הראשונית.

## סיום הריצה ובדיקת התוצאות

הריצה מסתיימת בסוף הסרטון. לעצירה מוקדמת אפשר ללחוץ על Q כאשר חלון התצוגה פעיל, או על Ctrl+C במסוף. המתינו לסיום התהליכים ולשמירת הקבצים.

לאחר הרצה תקינה, בתיקיית הפלט מופיעים:

| קובץ או תיקייה | תוכן |
|---|---|
| `results.json` | תוצאות לפי פריים, Pose, סיבות פסילה ואירועי זיהוי |
| `metrics.csv` | מדידות הביצועים לפי פריים |
| `runtime_summary.json` | הגדרות הריצה, ספירות פריימים, קודי יציאה ומצב יצירת הגרפים |
| `frame_XXXXXX.jpg` | תמונות פלט מדגמיות עם הסימון |
| `graphs/00_overview.png` | סקירה משולבת של שלושת הגרפים |
| `graphs/01_processing_time.png` | זמן עיבוד והשהיית תוצאה לאורך זמן |
| `graphs/02_reprojection_error.png` | שגיאת הקרנה לאורך זמן |
| `graphs/03_pose_availability.png` | זמינות Pose לאורך זמן |

הגרפים נשמרים גם בפורמט SVG, ולצדם קובצי הסבר וסטטיסטיקה. ביצירת הגרפים האוטומטית נדרשים לפחות שני פריימים שנשמרו וריצה שהסתיימה באופן תקין או בעצירה מסודרת. ריצה שנכשלה או שלא הושלמה עשויה לדלג על יצירתם.

ב-`runtime_summary.json` בדקו שבסיום רגיל קודי היציאה של `Video`, `Fast` ו-`Slow` הם `0`, ושערך `graphs.status` הוא `created`. כדי לוודא שהסרטון הושלם, בדקו גם שלא התבקשה עצירה ושלא הוגדרה מגבלת `--max-frames`.

`pose_valid=False` בחלק מהפריימים אינו בהכרח תקלה בהפעלת התוכנית. משמעותו שלא התקבל באותו פריים אומדן Pose שעבר את בדיקות המערכת. הסיבה נרשמת בתוצאות.

**המערכת אינה שומרת אוטומטית סרטון פלט מסומן.** היא מציגה את הפלט בזמן ההרצה ושומרת תמונות מדגמיות, מדדים וגרפים.

ליצירה מחדש של הגרפים מנתוני ריצה קיימת, בלי להריץ שוב זיהוי ומעקב:

```text
python -m metrics.export_report_figures --run outputs/run_v1
```

## חלק 1 - כיול וזיהוי המשטח

חלק זה כולל את `calibrate.py`, את `slow_detector.py` ואת `x_detector.py`.

### כיול

קובץ הכיול המצורף מאפשר להריץ את הסרטונים התואמים לו. הכיול הנוכחי מתייחס לתמונות מקור בגודל 3840 על 2160. שימוש במצלמה אחרת, בזום אחר או במצב צילום אחר מחייב כיול תואם; רזולוציה זהה לבדה אינה מבטיחה התאמה.

לשחזור חישוב הכיול מתוך התמונות הקיימות ב-`calibration/images`, עם הגדרות הכיול שנבחרו בפרויקט:

```text
python calibration/calibrate.py --square-size 1 --radial-order 1 --exclude frame_00660.png frame_00990.png --output calibration/camera_params_recomputed.npz --debug-dir outputs/calibration_recomputed
```

הפקודה שומרת את הכיול המחושב בשם חדש. `--square-size 1` משמעו שימוש ביחידת משבצת שרירותית עבור לוח המסך; קנה המידה המטרי של ה-Pose מתקבל ממידות דף A4. הפרטים המלאים נמצאים ב-[תיעוד הכיול](calibration/README.md).

### זיהוי ללא מעקב

```text
python -m detection.demo --video videos/v1.mp4 --camera-params calibration/camera_params.npz --sample-every 30 --max-frames 30 --output outputs/part1_detection
```

תוכנית זו דוגמת את הסרטון ואינה פותחת חלון תצוגה. הפלט לדוגמה זו נשמר ב-`outputs/part1_detection/v1/`. כאן `--max-frames` מגביל את מספר הדגימות לעיבוד, בשונה ממגבלת פריימי המקור ב-`main.py`. אימות ה-X מופעל מתוך הזיהוי ואין צורך להריץ את `x_detector.py` בנפרד.

## חלק 2 - מעקב וחישוב Pose

להדגמה רציפה של מעקב וחישוב מיקום ותנוחה:

```text
python -m tracking.demo --video videos/v1.mp4 --camera-params calibration/camera_params.npz --pose --output outputs/part2_tracking
```

הפלט נשמר ב-`outputs/part2_tracking/v1/`. זו תוכנית בדיקה רציפה של רכיבי המעקב וה-Pose, ללא חלון תצוגה וללא פיצול לשלושת תהליכי המערכת. הפקודה `main.py` היא זו שמפעילה את ההרצה המלאה עם תצוגה וגרפים אוטומטיים.

## חלק 3 - הרצה בשלושה תהליכים

הפקודות בפרק ההרצה המלאה מפעילות חלק זה. `Video` קורא ומציג, `Fast` עוקב ומחשב Pose, ו-`Slow` מבצע זיהוי מחדש. פרמטרי הכיול נטענים מהקובץ, והמערכת משתמשת ברכיבי הזיהוי והמעקב כחלק מההרצה.

תכנון התורים, הסנכרון והטיפול בתוצאות מאוחרות מתואר ב-[תיעוד שלושת התהליכים](docs/multiprocessing.md).

## פתרון בעיות נפוצות

| הודעה או מצב | מה לבדוק |
|---|---|
| `No module named cv2` או חבילה חסרה | התקינו `requirements.txt` באמצעות אותו Python שבו מריצים את העבודה. |
| `Video not found` | ודאו שהמסוף נמצא בתיקיית הפרויקט ושהסרטון קיים בנתיב שנמסר. |
| קובץ כיול חסר או לא תקין | ודאו שקיים `calibration/camera_params.npz`, או מסרו קובץ תואם עם `--camera-params`. |
| אי התאמה בין גודל הכיול לגודל הווידאו | השתמשו בצילום התואם לכיול. `--width` משנה את רזולוציית העבודה ואינו עוקף בדיקת התאמה של המקור. |
| הגרפים לא הופיעו | המתינו לסיום ושימו לב להודעת יצירת הגרפים ולשדה `graphs` בקובץ סיכום הריצה. |

## מסמכי העבודה

- [מסמך טכני מלא עם גרפים מוטמעים](docs/technical_report.html)
- [מקור המסמך הטכני לעריכה](docs/technical_report.md)
- [תיאורי שמונת הניסויים](docs/experiment_descriptions.md)

את קובץ ה-HTML אפשר לפתוח בדפדפן. לשמירה כ-PDF אפשר לבחור Ctrl+P ואז שמירה כ-PDF.
